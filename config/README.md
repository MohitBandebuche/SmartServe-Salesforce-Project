# Optional CLI project configuration

If you are importing this into an existing Salesforce DX project, keep the `force-app` folder and its metadata. You may generate a standard `sfdx-project.json` using Salesforce CLI:

sf project generate --name SmartServe

Then replace the generated force-app with the one in this package.
